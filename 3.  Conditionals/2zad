#include <iostream>

int main()
{
	unsigned year;
	std::cout << "Enter year" << std::endl;
	std::cin >> year;

	bool isLeap = ((year % 4 == 0 && year % 100 != 0 || year % 400 == 0));
		std::cout << "Is leap:" << (isLeap ? "Yes" : "No") << std::endl;


}
